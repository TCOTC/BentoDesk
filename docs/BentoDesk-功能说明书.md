# BentoDesk：轻量级 Windows 桌面整理工具——功能说明书

> 当前版本：1.3.0 | 开发者：Tianyu Zhu | 开源协议：GPL-3.0
> 技术栈：WinUI 3 + .NET 10 + Windows App SDK 2.2
> 适用系统：Windows 11（64 位）

---

## 一、BentoDesk 是什么

BentoDesk 是一款 Windows 11 桌面整理工具。它不替换你的桌面，而是在原生桌面之上补一层整理能力——用轻量的桌面"格子"来收纳文件、映射文件夹和控制音乐。

核心理念可以一句话概括：**文件仍然是普通文件，格子只是帮你收纳和快速访问。** 你不会被绑死在 BentoDesk 里——卸载它，你的文件还在原来的文件夹中，原封不动。

这与 Fences 等传统桌面整理工具的思路不同。Fences 倾向于"接管桌面"，把所有桌面图标纳入自己的管理体系；BentoDesk 更克制，它只在桌面上放置若干独立的小窗口（格子），每个格子有明确的功能边界，桌面其余部分完全不受影响。

> 📸 **[配图 1]** BentoDesk 桌面效果展示——一张铺满格子的桌面截图，包含文件格子、音乐格子等，体现"格子铺在桌面上"的整体观感。

---

## 二、安装

### 安装包

从官网或 GitHub Releases 下载 Inno Setup 构建的 exe 安装包（如 `BentoDesk_Setup_1.3.0_x64.exe`）。安装包自带独立更新器，可在应用内检查并安装新版本。

### 安装过程

安装到当前用户目录（非 Program Files），**不需要管理员权限**。这是一个刻意的设计——以管理员权限运行的桌面工具会导致资源管理器的拖拽功能失效，因此 BentoDesk 选择用户级安装。

安装时会自动检测两个运行时依赖：

- .NET 10 Desktop Runtime x64
- Windows App Runtime 2.2 x64

如果系统缺少，安装器会联网下载并安装。

覆盖安装会保留已有的设置、格子配置和收纳目录中的文件。如果旧版本装在 Program Files 下，安装器会自动迁移到用户目录。

### 卸载

卸载前会先停止运行中的 BentoDesk，然后询问是否删除本地数据（位于 `%LocalAppData%\BentoDesk`）。收纳目录中的用户文件不会被静默删除，涉及文件操作前会提示确认。

> 📸 **[配图 2]** 安装完成后的首次启动界面——品牌 logo 入场动画的一帧。

---

## 三、新用户引导

首次启动 BentoDesk 时会弹出引导窗口（Mica 背景材质，1040×740），带你完成 6 步初始设置。每步都有进度圆点指示，可以随时跳过。

### 第 1 步：欢迎

品牌动效展示，简述 BentoDesk 的定位——在桌面上放格子来整理文件和功能。

### 第 2 步：收纳目录与快速访问

这是最关键的一步。BentoDesk 需要一个"收纳根路径"——所有"收纳格子"背后的真实文件夹都存放在这里。默认路径是 `%UserProfile%\BentoDesk`，你可以改成任意位置。

> 💡 **理解收纳目录**：如果你创建了一个叫"工作文件"的收纳格子，BentoDesk 会在收纳目录下创建一个 `工作文件` 子文件夹。拖入格子的文件默认复制到这个子文件夹，也可以在设置中改为移动。在资源管理器中打开收纳目录，你就能看到所有格子对应的文件夹。

这一步还有一个开关：把收纳目录固定到资源管理器的"快速访问"。这样你在资源管理器左侧栏就能直接访问收纳目录，不用一层层翻路径。如果你后续修改了收纳路径，BentoDesk 会提示你迁移已有的格子数据。

> 📸 **[配图 3]** 引导第 2 步截图——收纳路径设置界面，展示路径输入框和"固定到快速访问"开关。

### 第 3 步：外观

三组选择，右侧有实时预览：

- **主题**：跟随系统 / 浅色 / 深色
- **强调色**：系统色 或 6 个预设色（蓝、红、绿、紫、橙、青）
- **材质**：云母 / 云母 Alt / 轻薄亚克力 / 标准亚克力 / 纯色不透明

这一步的设置和后续设置页中的外观选项完全同步，改了立刻生效。

> 📸 **[配图 4]** 引导第 3 步截图——外观选择界面，展示主题、强调色、材质三组选项和右侧实时预览。

### 第 4 步：功能格子

两个开关，决定启用哪些功能：

- ☑️ 音乐
- ☑️ 搜索

开了哪个，后续就能在托盘菜单里创建对应类型的格子，或使用对应能力。不开的话，相关入口会隐藏。这个设置也可以随时在设置页中修改。

> 📸 **[配图 5]** 引导第 4 步截图——功能格子选择界面，两个卡片式开关。

### 第 5 步：日常使用

两个开关：

- **全局快捷键**：默认开启，默认快捷键 F7。点击录制按钮后直接按键捕获，按 Esc 取消。这个快捷键的功能是一键显示/隐藏全部格子。
- **开机自启**：注册到 Windows 启动项，开机后 BentoDesk 自动运行并常驻托盘。

> 📸 **[配图 6]** 引导第 5 步截图——快捷键录制和开机自启开关界面。

### 第 6 步：就绪总结

汇总你刚才的所有选择——收纳路径及是否已固定、主题和材质、启用了哪些功能格子、快捷键和自启状态。确认无误后点"开始使用"，引导完成。

---

## 四、托盘菜单——日常控制中心

引导结束后，BentoDesk 常驻系统托盘。你不和格子直接交互时，它安静地待在托盘区。

### 左键单击：显示/隐藏全部格子

智能判断当前状态：如果格子已经显示，且前台窗口是桌面、任务栏或 BentoDesk 自身，则隐藏全部格子；否则唤起全部格子到顶层。

这个操作也可以用全局快捷键（默认 F7）触发。日常使用中最频繁的动作就是这个——需要格子时唤起，不需要时收起，桌面瞬间回到干净状态。

### 右键菜单

> 📸 **[配图 7]** 托盘右键菜单截图——展示完整的菜单项列表。

| 菜单项 | 功能 |
|--------|------|
| **创建格子** | 展开子菜单，列出当前可创建的格子类型（根据功能格子开关动态显示） |
| **映射文件夹** | 弹出文件夹选择器，选择一个已有文件夹映射为桌面格子（只读展示，不移动文件） |
| **打开收纳目录** | 在资源管理器中打开收纳根路径 |
| **检查更新** | 有新版本时才会显示此项 |
| **设置** | 打开设置窗口 |
| **退出** | 退出 BentoDesk |

托盘图标本身也有样式可调——系统默认 / 彩色 / 黑色 / 白色，可设为跟随主题自动切换。

---

## 五、主要格子类型

格子是 BentoDesk 的核心交互单元。每个格子是一个独立的桌面小窗口，有自己的标题栏、内容和操作菜单。当前主要包括文件格子与音乐格子；搜索等功能格子也可按需启用。

> 📸 **[配图 8]** 格子并列展示——一张图里同时展示文件格子与音乐格子。

### 5.1 文件格子

文件格子是 BentoDesk 最核心的格子类型，用于收纳和整理桌面文件。它有两种来源：

**收纳格子**

背后对应一个真实文件夹（位于收纳目录下）。当你把文件拖入格子时，文件默认复制到这个文件夹中，也可在设置里改为移动。无论选择哪种方式，格子展示的都是普通 Windows 文件。

创建方式：托盘菜单 → 创建格子 → 选择文件格子。

**映射格子**

指向一个已有的文件夹（可以是任意位置，不限于收纳目录）。映射格子只读展示文件夹内容，不会移动任何文件。适合那些你不想移动但希望在桌面上快速访问的文件夹。

创建方式：托盘菜单 → 映射文件夹 → 选择目标文件夹。

> 💡 **收纳 vs 映射的区别**：收纳格子会把拖入内容复制或移动到 BentoDesk 管理的真实文件夹；映射格子直接展示你指定的已有文件夹，不改变文件位置。

**文件操作**

| 操作 | 方式 |
|------|------|
| 拖入文件 | 从资源管理器或桌面拖文件到格子，默认复制，可在设置中改为移动 |
| 拖出文件 | 从格子拖文件到资源管理器或其他位置 |
| 双击打开 | 双击文件项打开（可在设置中关闭此行为） |
| 空格预览 | QuickLook 已运行时，选中文件后按 Space 打开或关闭预览 |
| 右键菜单 | 复制、剪切、重命名、删除、在资源管理器中显示 |
| 多选 | 框选或 Ctrl/Shift 点击多选，支持批量复制/剪切/删除 |
| 排序 | 名称 / 大小 / 类型 / 修改日期 |
| 视图切换 | 图标视图 / 列表视图（列表视图可显示详情） |

**自动叠放**

自动叠放只改变格子内的展示，不移动真实文件。可以按文件类型或日期分组，也可以创建自定义扩展名规则。自定义规则从上到下匹配，支持拖动调整优先级、实时预览命中文件、设置形成叠放所需数量、叠放内排序，以及未匹配文件保持散开或收入“其他”。展开叠放后，文件仍在当前格子内散开展示。

> 📸 **[配图 9]** 文件格子截图——展示图标视图下的文件格子，包含若干文件项和标题栏操作按钮。

> 📸 **[配图 10]** 文件格子右键菜单——展示排序、视图切换等菜单项。

### 5.2 音乐格子

音乐格子是一个桌面音乐控制面板。注意：它本身不是播放器，而是对系统正在播放的音乐进行控制。

**功能**

- 播放控制：上一首 / 播放暂停 / 下一首
- 播放模式切换
- 系统音量调节
- 封面居中裁剪展示
- 歌名跑马灯（文字过长时滚动）
- 封面平滑切换动画

**自适应布局**

默认会根据格子尺寸自动切换布局；也可在设置中强制使用封面模式或控制模式。自动模式在宽度小于 180px 时切换为封面优先的紧凑布局，只显示封面和基本控制，隐藏歌名等次要信息。

**氛围取色**

可开启"封面氛围"功能，根据当前播放歌曲的封面颜色，为格子背景生成柔和的色调，营造沉浸感。

**悬停动效**

鼠标悬停在封面上时有轻量的动效反馈。

> 📸 **[配图 12]** 音乐格子截图——展示封面、歌名、播放控制按钮和氛围取色背景。

> 💡 音乐格子是**单例格子**——全局只能创建一个。重复创建不会生成新的，而是聚焦到已有的那个。

---

## 六、格子窗口交互

所有类型的格子共享一套统一的窗口交互行为。

### 标题栏

每个格子顶部有一个标题栏，显示格子名称。标题栏支持以下操作：

- **拖动移动**：按住标题栏拖动格子到任意位置
- **双击重命名**：双击标题栏进入重命名模式
- **右键菜单**：重命名 / 切换视图 / 排序 / 锁定位置 / 锁定大小 / 添加格子 / 更多 / 删除
- **悬停操作按钮**：鼠标移到标题栏时显示快捷操作按钮（可在设置中配置显示哪些按钮：锁定位置、锁定大小、添加、更多、删除）

### 调整大小

拖动格子边缘可以调整大小。调整过程中有**对齐引导线**——当格子边缘接近其他格子边缘或屏幕工作区边缘时（8px 阈值），会出现高亮对齐线，帮助你对齐格子。可在设置中开关此功能。

格子最小尺寸为 150×150。

### 位置锁定

可以锁定格子的位置和大小。锁定后无法拖动或调整，防止误操作。需要解锁后才能再次移动。

> 📸 **[配图 15]** 格子调整大小时的对齐引导线——展示两个格子边缘对齐时的高亮线效果。

### 胶囊模式

开启“允许格子收起”后，格子可以收起为紧凑胶囊。常用选项包括：

- **展开方式**：保持展开、点击展开或悬停展开。悬停仅由标题中间区域触发，图标与操作区保留给拖动和按钮，减少误展开。
- **收起后显示**：关键信息、简要摘要或仅图标和标题；可隐藏敏感正文。
- **宽度关系**：保持同宽（胶囊和展开格子宽度一致）或独立调整。
- **排列方式**：独立摆放，或组合成可整体移动和排序的栏；组合栏可悬浮或贴到屏幕边缘。
- **动画与响应**：快速、标准、舒缓、关闭或自定义动画；悬停响应支持灵敏、平衡、防误触和自定义。

格子会以胶囊所在边角为展开锚点，并结合屏幕剩余空间选择方向。拖入文件、文本或附件时会临时展开，松开或离开后恢复原状态，不会修改原有展开尺寸或胶囊位置。单个格子可覆盖全局展开方式、宽度和位置，这些覆盖项可在设置中集中恢复为跟随全局。

### 层级管理

BentoDesk 提供两种格子层级模式（设置 → 交互中配置）：

**动态模式（默认）**

格子默认位于桌面层（z-order 较低），不遮挡其他窗口。需要操作格子时，点击格子标题栏或按全局快捷键，格子会被提升到顶层。当格子失去焦点后，自动回落到桌面层。

这种模式的好处是格子平时不挡事，需要时才浮上来。

**桌面固定模式**

格子始终钉在桌面层，不会被提升到顶层。适合你希望格子始终作为桌面背景一部分的场景。

### 标题模式

可分别配置"显示类格子"（文件）和"交互类格子"（音乐）的标题显示模式：

- **标准**：正常显示标题栏
- **紧凑**：缩小标题栏高度
- **覆盖**：标题栏覆盖在内容上方
- **隐藏**：完全不显示标题栏

### 多显示器支持

格子记录了创建时的显示器和位置锚点（左上/右上/左下/右下）。当分辨率变化或显示器拓扑发生变化时（如外接/断开显示器），格子会根据锚点自动恢复到合理位置。

> 📸 **[配图 16]** 多显示器场景——展示格子分布在两个显示器上的效果。

---

## 七、设置

通过托盘菜单 → 设置，或格子标题栏右键 → 更多 → 设置打开。设置采用接近 Windows 11 的左侧导航、标题栏全局搜索和聚焦详细页面；常用选项直接放在入口卡片，复杂参数进入三级页面。

> 📸 **[配图 17]** 设置窗口全貌——左侧导航栏、标题栏搜索和右侧设置内容区。

### 7.1 常规（General）

| 设置项 | 说明 |
|--------|------|
| 主题 | 跟随系统 / 浅色 / 深色 |
| 托盘图标样式 | 系统 / 彩色 / 黑色 / 白色，可设跟随主题 |
| 语言 | 跟随系统 / 简体中文 / English |
| 强调色 | 系统色 或 自定义（蓝/红/绿/紫/橙/青） |

> 📸 **[配图 18]** 常规设置页截图。

### 7.2 外观（Appearance）

这是设置项最多的一个分区，控制格子的视觉表现。

**材质与边框**

| 设置项 | 选项 |
|--------|------|
| 材质 | 云母 / 云母 Alt / 轻薄亚克力 / 标准亚克力 / 纯色 |
| 材质浓度 | 独立调节原生材质色调与层次，不改变透明度 |
| 边框颜色 | 中性 / 主题色 / 无 |
| 边框样式 | 细 / 中 / 粗 |
| 圆角 | 默认（系统） / 方形 / 小圆 / 圆 |
| 透明度 | 滑块调节（0-100%） |

**标题与图标**

| 设置项 | 选项 |
|--------|------|
| 标题图标模式 | 填充单色 / 线性单色 / 彩色 / 隐藏 / 文字标签 |
| 文字大小 | 滑块调节 |
| 图标大小 | 滑块调节 |
| 间距比例 | 水平间距 / 垂直间距 / 文件名宽度（滑块调节） |
| 文件名显示扩展名 | 开关 |
| 快捷方式隐藏箭头 | 开关 |

**布局密度**

紧凑 / 标准 / 宽松 / 自定义。修改任意单独密度参数后自动变为自定义，文件列表实时联动。

**外观细节页**

还有一个"外观细节"子页面，提供更精细的微调参数，供高级用户调整。

> 📸 **[配图 19]** 外观设置页截图——展示材质、边框、圆角、透明度等设置项和预览效果。

### 7.3 胶囊模式（Capsule Mode）

胶囊模式页控制是否允许格子收起，以及展开方式、收起后内容、隐私、宽度关系和排列方式。首页只展示高频选择；悬停延迟、动画时长、组合栏间距/方向和单格覆盖管理进入详细页面。关闭总开关不会清空配置，再次开启时恢复原设置。

### 7.4 文件格子（FileWidgets）

控制文件格子的行为细节。

| 设置项 | 说明 |
|--------|------|
| 拖入行为 | 复制（默认）/ 移动 |
| 双击打开 | 是否允许双击文件项直接打开 |
| 列表显示详情 | 列表视图下是否显示文件大小、修改日期等详情 |
| 悬停操作按钮 | 是否在悬停时显示操作按钮 |
| 操作按钮配置 | 勾选显示哪些按钮：锁定位置 / 锁定大小 / 添加 / 更多 / 删除 |
| 调整大小对齐引导 | 开关 |
| 文件项路径提示 | 鼠标悬停时是否显示文件完整路径 |
| 自动叠放 | 按类型、日期或自定义扩展名规则整理展示 |
| QuickLook | 无设置项；QuickLook 已运行时按 Space 被动调用预览 |

> 📸 **[配图 20]** 文件格子设置页截图。

### 7.5 功能格子（FeatureWidgets）

音乐、搜索等功能格子的总开关，以及各自的子设置。

**音乐子设置**

| 设置项 | 说明 |
|--------|------|
| 封面氛围 | 开关——是否根据封面取色做背景 |
| 悬停动效 | 开关——封面悬停动效 |
| 显示模式 | 自动 / 封面模式 / 控制模式 |

> 📸 **[配图 21]** 功能格子设置页截图——展示功能格子的开关和展开的子设置。

### 7.6 交互（Interaction）

| 设置项 | 说明 |
|--------|------|
| 全局快捷键 | 开关 + 自定义录制 |
| 格子层级模式 | 动态 / 桌面固定 |
| 显示类格子标题模式 | 标准 / 紧凑 / 覆盖 / 隐藏 |
| 交互类格子标题模式 | 标准 / 紧凑 / 覆盖 / 隐藏 |
| 唤起时仅聚焦当前格子 | 开关——快捷键唤起时是否只聚焦到鼠标下的格子 |
| 悬停按钮行为 | 配置悬停时的交互行为 |

> 📸 **[配图 22]** 交互设置页截图。

### 7.7 维护（Maintenance）

这个分区是 BentoDesk 的"工具箱"，提供诊断和修复功能。

| 功能 | 说明 |
|--------|------|
| 收纳根路径 | 查看和修改当前收纳路径 |
| 快速访问固定状态 | 查看和管理资源管理器快速访问的固定状态 |
| 孤立收纳文件夹恢复 | 恢复因异常情况变成"孤立"的收纳文件夹（格子配置丢失但文件夹还在） |
| 拖拽异常诊断 | 诊断拖拽功能是否正常，一键修复常见问题 |
| 数据备份与恢复 | 导出/恢复带完整性校验的 ZIP，并管理自动快照和恢复前快照 |
| 全局重置 | 恢复所有偏好设置为默认值（保留语言、开机启动、功能格子开关） |
| 功能格子单独重置 | 单独重置某个功能格子的数据和设置（需二次确认） |

> 💡 **全局重置 vs 功能格子重置**：全局重置只恢复"偏好设置"（外观、交互等），不会删除你的文件。功能格子重置会清除对应格子的所有数据，需要二次确认。

> 📸 **[配图 23]** 维护设置页截图。

### 7.8 关于（About）

| 内容 | 说明 |
|------|------|
| 版本信息 | 当前版本号 |
| 检查更新 | 手动检查 + 自动检查开关 |
| GitHub 仓库 | 链接到开源仓库 |
| 官网 | 链接到产品官网 |
| 公众号二维码 | 关注渠道 |
| 捐赠二维码 | 微信 / 支付宝捐赠入口 |

> 📸 **[配图 24]** 关于页截图。

---

## 八、如何用 BentoDesk 整理桌面

这一节不是功能罗列，而是基于 BentoDesk 的设计理念，给出实际的使用建议。

### 核心原则

BentoDesk 的设计哲学是"不替换桌面，只补一层"。意思是：桌面只放当前正在用的东西，其他都收进格子；格子可以随时唤起和隐藏，文件始终是普通文件，始终在真实文件夹里，不会和 BentoDesk 绑死。

### 推荐工作流

**第一步：按用途创建收纳格子**

打开托盘菜单 → 创建格子，为不同用途创建文件格子。比如：

- 📁 工作文件——日常工作文档
- 📁 下载临时——从浏览器下载的东西先丢这里
- 📁 截图——截图工具的默认保存目标
- 📁 项目素材——当前项目的图片、资料
- 📁 待处理——需要后续跟进的文件

每个格子背后是一个真实文件夹。默认拖入会复制文件；如果希望桌面原文件在收纳后消失，可把拖入行为改为“移动”。

**第二步：对不想移动的文件夹用映射格子**

有些文件夹你不希望移动（比如项目目录、代码仓库），但又想在桌面上快速访问。用托盘菜单 → 映射文件夹，把它映射成桌面格子。映射格子只读展示，不碰原文件。

**第三步：放置功能格子**

根据需要开启功能格子：

- 放一个**搜索格子**，快速找回桌面和收纳目录里的文件。
- 放一个**音乐格子**，工作时控制播放不用切窗口。

**第四步：善用快捷键**

记住全局快捷键（默认 F7）。工作时按一下唤起全部格子，用完再按一下隐藏。桌面在"工作模式"（格子全开）和"干净模式"（格子全收）之间一键切换。

**第五步：收纳目录固定到快速访问**

确保收纳目录已经固定到资源管理器的"快速访问"（引导第 2 步或设置 → 维护中可以操作）。这样在资源管理器里也能直接访问格子里收纳的文件，不用通过 BentoDesk。

> 📸 **[配图 25]** 整理后的桌面效果——展示一个"工作模式"下的桌面，几个收纳格子有序排列，角落有音乐格子。

### 注意事项

1. **不要把所有文件塞进一个格子**。格子是分类工具，一个格子对应一个用途。格子太多反而乱，建议 4-8 个文件格子。
2. **映射格子不会改变文件位置**。如果你希望文件进入 BentoDesk 管理的收纳结构，请使用收纳格子；如需同时移除来源文件，再把拖入行为设为“移动”。
3. **音乐格子是单例**。全局只能有一个，重复创建不会生成新的。
4. **卸载 BentoDesk 不会丢文件**。收纳格子里的文件都在收纳目录的真实文件夹中，卸载 BentoDesk 只是删掉程序和配置，文件原封不动。
5. **如果拖拽不工作**，去设置 → 维护 → 拖拽异常诊断，一键修复。常见原因是程序以管理员权限运行（BentoDesk 默认用户级运行，一般不会出这个问题）。

---

## 九、技术概览

> 本节面向技术人员，普通用户可跳过。

### 技术栈

| 层面 | 技术 |
|------|------|
| UI 框架 | WinUI 3 + Windows App SDK 2.2 |
| 运行时 | .NET 10 |
| MVVM | CommunityToolkit.Mvvm（源生成器模式） |
| UI 控件 | CommunityToolkit.WinUI |
| 托盘图标 | H.NotifyIcon.WinUI |
| 打包 | Inno Setup（Direct）/ MSIX（Store） |
| 多语言 | 自研 LocalizationService（JSON 字典） |

### 架构

采用 MVVM 分层架构：

- **Models**：AppSettings、WidgetConfig、FileStackCustomRule 等数据模型
- **ViewModels**：按格子和设置领域拆分，音乐、搜索与文件叠放分别维护状态
- **Views**：窗口生命周期、菜单、拖放、选择、调整大小、胶囊状态和标题交互按职责拆分为 partial class
- **Services**：WidgetManager、文件操作、附件、自动叠放、备份恢复、QuickLook、音乐会话、提醒、设置、主题和全局热键等
- **Helpers**：Win32 互操作、Shell 上下文菜单、图标提取、剪贴板等

### 分发

通过 Inno Setup 安装包分发，应用内更新由 `BentoDesk.Updater` 与安装包覆盖安装完成。

### 项目规模

- 主应用按窗口、内容、设置和服务职责拆分，避免继续堆积超大界面类
- 自动化测试覆盖数据安全、附件、叠放、胶囊边界、设置迁移和核心格子行为
- Direct 安装器不内置 .NET 与 Windows App Runtime 安装程序，缺失时按需下载

---

## 附录：快捷参考表

### 托盘操作

| 操作 | 动作 |
|------|------|
| 左键单击 | 显示/隐藏全部格子 |
| 右键单击 | 打开托盘菜单 |
| 全局快捷键（默认 F7） | 显示/隐藏全部格子 |

### 格子操作

| 操作 | 动作 |
|------|------|
| 拖动标题栏 | 移动格子 |
| 双击标题栏 | 重命名格子 |
| 右键标题栏 | 格子菜单 |
| 拖动边缘 | 调整大小（有对齐引导） |
| 悬停标题栏 | 显示操作按钮 |
| 点击收起按钮 | 在展开格子和胶囊之间切换 |

### 文件格子操作

| 操作 | 动作 |
|------|------|
| 拖入文件 | 默认复制（或按设置移动）到格子对应文件夹 |
| 拖出文件 | 移动到目标位置 |
| 双击文件 | 打开文件 |
| 右键文件 | 复制/剪切/重命名/删除/在资源管理器中显示 |
| Ctrl+点击 | 多选 |
| 框选 | 拉框多选 |
| Space | QuickLook 已运行时预览选中文件 |
| 点击叠放 | 在格子内展开或收回自动叠放 |

---

*BentoDesk v1.3.0 | 开源协议 GPL-3.0 | GitHub: github.com/...</sup>*
